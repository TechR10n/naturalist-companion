# Required (Vertex AI)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: if not using `gcloud auth application-default login`
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Optional: override notebook defaults
VERTEX_LLM_MODEL=gemini-flash-latest
VERTEX_EMBEDDING_MODEL=text-embedding-005

# Optional: Wikipedia slice tuning (used by scripts / notebook defaults)
WIKIPEDIA_QUERY=Interstate 81 OR Shenandoah Valley OR Roanoke OR Appalachian Mountains
WIKIPEDIA_MAX_DOCS=10
WIKIPEDIA_TOP_K=3

# Optional: vector store choice
# - chroma: easiest local default
# - faiss: included in provider profiles (`requirements-*-dev.txt`) and project extras
VECTORSTORE=chroma
CHROMA_PERSIST_DIR=./.chroma
